/**
 * VIXUAL - Rules Engine V1 (alias officiel)
 *
 * Ce fichier est le nom officiel du moteur de règles VIXUAL.
 * Il re-exporte simplement le contenu historique `lib/visual-rules-engine.ts`
 * afin que les nouveaux imports utilisent la nomenclature finale
 * sans casser les imports existants.
 *
 * À terme, `lib/visual-rules-engine.ts` pourra être supprimé
 * après migration de tous les imports vers ce fichier.
 */

export * from "./visual-rules-engine";
